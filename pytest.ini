[pytest]
testpaths = tests
# Make imports such as `from src.common.collection import ...` work when tests
# are launched from VS Code or a subdirectory instead of the project root.
pythonpath = .
addopts = -m "not integration"
markers =
    integration: requires Azure credentials and access to a real storage account
